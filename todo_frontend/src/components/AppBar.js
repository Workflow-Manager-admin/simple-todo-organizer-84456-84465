import React from "react";

// PUBLIC_INTERFACE
function AppBar() {
  /** App bar with app title, calendar icon placeholder */
  return (
    <div className="appbar">
      <span className="appbar-title">TODO APP</span>
      <span className="appbar-calendar"></span>
    </div>
  );
}

export default AppBar;
